# Container Creation
Check the folders for which OS you want to create your container, be mindful CMD and Powershell are different on Windows. Also there are two example `.ps1` files saved as `.txt` files so you can copy paste the contents into a `rungazebo.ps1` file for easy acces and correct instalment of packages everytime you enter the container.
